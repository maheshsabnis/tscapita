export function  sumation(x:number,y:number):number {
    return x + y;
}

export function  substrctaion(x:number,y:number):number {
    return x - y;
}

export function  multiply(x:number,y:number):number {
    return x * y;
}